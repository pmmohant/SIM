package com.lavante.sim.customer.pageobjects.Transactions.PromoAllowances;

import java.util.LinkedHashMap;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.testng.Assert;
import org.testng.Reporter;

import com.lavante.sim.Common.Util.LavanteUtils;

public class PromoClaim {
	LavanteUtils lavanteUtils = new LavanteUtils();

	public PromoClaim(WebDriver driver) {
		lavanteUtils.driver = driver;
	}

	
	/**
	 * @author Piramanayagam.S
	 * 
	 * @param dataMap
	 */
	private void createClaim(LinkedHashMap<String, String> dataMap) {
		if (dataMap.containsKey("ClaimType")) {
			String x=dataMap.get("ClaimType");
			
	//		lavanteUtils.typeinEdit(x, EnterCommentsTxtArea);
		}
		if (dataMap.containsKey("PrivateComment")) {
	//		lavanteUtils.clickJavaScriptNoWait(privateRadioBtn);
		}
		
	}	
}
